# 005. Go 控制流：条件判断与循环

本章核心目标：**掌握 Go 的条件语句、循环语句和延迟执行机制**。

---

## 1. 概述

Go 的控制流设计简洁而强大：

| 语句类型 | Go 的实现 | 特点 |
| --- | --- | --- |
| 条件判断 | `if-else`、`switch` | if 可带初始化语句；switch 不需要 break |
| 循环 | `for`（唯一循环） | 一个关键字实现所有循环形式 |
| 延迟执行 | `defer` | 函数返回前执行，用于资源清理 |

**Go 没有的**：`while`、`do-while`、三元运算符 `? :`

---

## 2. if-else 条件语句

### 2.1 基本语法

```go
if 条件 {
    // 条件为 true 时执行
}
```

**注意**：条件不需要括号，但花括号是必须的。

```go
// ✅ 正确
if age >= 18 {
    fmt.Println("成年")
}

// ❌ 错误：花括号不能省略
if age >= 18
    fmt.Println("成年")
```

### 2.2 复合条件与逻辑运算符

当需要判断多个条件时，使用以下逻辑运算符：

| 运算符 | 说明 | 示例 |
| :---: | --- | --- |
| `&&` | **逻辑与** (AND) | `if age > 18 && hasID` (两个都为 true) |
| `\|\|` | **逻辑或** (OR) | `if isWeekend \|\| isHoliday` (任一为 true) |
| `!` | **逻辑非** (NOT) | `if !isLogin` (取反) |

**短路求值**：
- `A && B`：如果 A 为 false，则不再计算 B。
- `A || B`：如果 A 为 true，则不再计算 B。

```go
if user != nil && user.IsActive {
    // 只有当 user 不是 nil 时，才会检查 IsActive
    // 避免了空指针解引用 panic
    fmt.Println("用户活跃")
}
```

### 2.3 if-else

```go
score := 75
if score >= 60 {
    fmt.Println("通过")
} else {
    fmt.Println("未通过")
}
```

### 2.3 if-else if-else 链

```go
grade := 85
if grade >= 90 {
    fmt.Println("A")
} else if grade >= 80 {
    fmt.Println("B")
} else if grade >= 70 {
    fmt.Println("C")
} else if grade >= 60 {
    fmt.Println("D")
} else {
    fmt.Println("F")
}
```

### 2.4 带初始化语句的 if（Go 特有）

这是 Go 的一个优雅特性，可以在 if 中声明局部变量：

```go
if n := 10; n%2 == 0 {
    fmt.Printf("%d 是偶数\n", n)
} else {
    fmt.Printf("%d 是奇数\n", n)
}
// n 在 if-else 块外不可访问
```

**语法结构**：

```go
if 初始化语句; 条件 {
    // ...
}
```

### 2.5 Go 错误处理惯用法

#### 💡 核心概念：函数多返回值

在看代码之前，必须先理解 Go 的一个**核心特性**：
Go 的函数可以同时返回**多个**独立的值。

```go
func divide(a, b int) (int, error) {
    if b == 0 {
        return 0, errors.New("除数不能为0")
    }
    return a / b, nil
}
```

**语法拆解**：

Go 的函数声明非常独特，请看下面的结构：

`func divide(a, b int) (int, error)`

1.  **`(a, b int)`**：这是**输入参数列表**。
    *   表示函数接收两个参数 `a` 和 `b`，它们都是 `int` 类型。
2.  **`(int, error)`**：这是**返回值列表**。
    *   表示函数执行结束后，会返回两个值：一个是 `int`（结果），一个是 `error`（错误信息）。

这种写法让函数签名非常清晰：一眼就能看出"输入什么"和"输出什么"。

#### 实际应用

结合 `if` 的初始化语句，这就形成了 Go 最经典的错误处理模式：

```go
// 标准写法
result, err := someFunction()
if err != nil {
    // 处理错误
    return err
}
// 使用 result

// 更紧凑的写法（初始化 + 判断）
if result, err := someFunction(); err != nil {
    return err
} else {
    // 使用 result
}
```

---

## 3. switch 语句

Go 的 switch 比其他语言更强大、更安全。

### 3.1 基本用法

```go
day := 3
switch day {
case 1:
    fmt.Println("星期一")
case 2:
    fmt.Println("星期二")
case 3:
    fmt.Println("星期三")
default:
    fmt.Println("其他")
}
```

**关键区别**：Go 的 switch **不需要 break**！每个 case 执行完自动退出。

### 3.2 多值匹配

Go 的 `case` 可以同时匹配多个值，使用逗号分隔。

```go
switch time.Now().Weekday() {
case time.Saturday, time.Sunday:
    fmt.Println("It's weekend")
default:
    fmt.Println("It's workday")
}
```

这比写多个 `if-else` 或重复的 `case` 简洁得多。

### 3.3 带初始化语句的 switch

```go
switch num := calculate(); {
case num < 0:
    fmt.Println("负数")
case num == 0:
    fmt.Println("零")
case num > 0:
    fmt.Println("正数")
}
```

### 3.4 无表达式 switch

省略表达式时，switch 相当于 `if-else if` 链，case 可以是任意布尔表达式：

```go
hour := 14
switch {
case hour < 12:
    fmt.Println("上午好")
case hour < 18:
    fmt.Println("下午好")
default:
    fmt.Println("晚上好")
}
```

这种形式比多个 `else if` 更清晰。

### 3.5 fallthrough：穿透到下一个 case

如果需要继续执行下一个 case（类似其他语言不写 break 的行为），使用 `fallthrough`：

```go
level := 1
switch level {
case 1:
    fmt.Print("读取 ")
    fallthrough
case 2:
    fmt.Print("写入 ")
    fallthrough
case 3:
    fmt.Print("执行")
}
// 输出: 读取 写入 执行
```

**注意**：`fallthrough` 是无条件执行下一个 case，不会检查条件。

### 3.6 类型 switch

用于检查接口变量的实际类型：

```go
func checkType(x interface{}) {
    switch v := x.(type) {
    case int:
        fmt.Printf("整数: %d\n", v)
    case string:
        fmt.Printf("字符串: %s\n", v)
    case float64:
        fmt.Printf("浮点数: %f\n", v)
    default:
        fmt.Printf("未知类型: %T\n", v)
    }
}

checkType(42)        // 整数: 42
checkType("hello")   // 字符串: hello
checkType(3.14)      // 浮点数: 3.140000
```

---

## 4. for 循环

Go 只有一个循环关键字 `for`，但它可以实现所有循环形式。

### 4.1 标准三段式

```go
for 初始化; 条件; 后续操作 {
    // 循环体
}
```

```go
for i := 0; i < 5; i++ {
    fmt.Println(i)
}
```

### 4.2 while 风格

省略初始化和后续操作，只保留条件：

```go
count := 0
for count < 5 {
    fmt.Println(count)
    count++
}
```

### 4.3 无限循环

省略所有部分：

```go
for {
    // 无限循环，通常配合 break 使用
    if condition {
        break
    }
}
```

### 4.4 for range 遍历

`for range` 是遍历集合的标准方式。

**遍历切片/数组**：

```go
nums := []int{10, 20, 30}

// 获取索引和值
for i, v := range nums {
    fmt.Printf("索引 %d: 值 %d\n", i, v)
}

// 只要值（用 _ 忽略索引）
for _, v := range nums {
    fmt.Println(v)
}

// 只要索引
for i := range nums {
    fmt.Println(i)
}
```

**遍历字符串**（按 Unicode 字符）：

```go
str := "Go语言"
for i, r := range str {
    fmt.Printf("索引 %d: 字符 %c\n", i, r)
}
// 索引 0: 字符 G
// 索引 1: 字符 o
// 索引 2: 字符 语
// 索引 5: 字符 言
```

**注意**：索引是字节位置，中文字符占 3 个字节（UTF-8）。

**遍历 map**：

```go
ages := map[string]int{"Alice": 25, "Bob": 30}
for name, age := range ages {
    fmt.Printf("%s: %d\n", name, age)
}
```

**注意**：map 遍历顺序是随机的！

### 4.5 循环形式对比

| 形式 | 语法 | 等价于 |
| --- | --- | --- |
| 标准循环 | `for i := 0; i < n; i++` | C/Java 的 for |
| while 循环 | `for condition` | 其他语言的 while |
| 无限循环 | `for { }` | `while(true)` |
| 遍历集合 | `for i, v := range collection` | foreach |

---

## 5. 循环控制

### 5.1 break：跳出循环

```go
for i := 0; i < 10; i++ {
    if i == 5 {
        break  // 跳出循环
    }
    fmt.Println(i)
}
// 输出: 0 1 2 3 4
```

### 5.2 continue：跳过当前迭代

```go
for i := 0; i < 10; i++ {
    if i%2 == 0 {
        continue  // 跳过偶数
    }
    fmt.Println(i)
}
// 输出: 1 3 5 7 9
```

### 5.3 带标签的 break/continue

用于嵌套循环时，控制外层循环：

**跳出外层循环**：

```go
outer:
for i := 0; i < 3; i++ {
    for j := 0; j < 3; j++ {
        if i == 1 && j == 1 {
            break outer  // 跳出外层循环
        }
        fmt.Printf("i=%d, j=%d\n", i, j)
    }
}
```

**继续外层循环的下一次迭代**：

```go
nextRow:
for i := 0; i < 3; i++ {
    for j := 0; j < 3; j++ {
        if j == 1 {
            continue nextRow  // 跳到外层循环的下一次迭代
        }
        fmt.Printf("i=%d, j=%d\n", i, j)
    }
}
```

---

## 6. defer 延迟执行

`defer` 是 Go 的独特特性，用于在函数返回前执行清理操作。

### 6.1 基本用法

```go
func example() {
    defer fmt.Println("最后执行")
    fmt.Println("先执行")
    fmt.Println("再执行")
}
// 输出:
// 先执行
// 再执行
// 最后执行
```

### 6.2 多个 defer：LIFO（后进先出）

```go
func stack() {
    defer fmt.Println("第一个 defer")
    defer fmt.Println("第二个 defer")
    defer fmt.Println("第三个 defer")
    fmt.Println("函数主体")
}
// 输出:
// 函数主体
// 第三个 defer
// 第二个 defer
// 第一个 defer
```

**记忆方法**：defer 像栈一样，后进先出。

### 6.3 参数在声明时求值

```go
func evalDemo() {
    x := 10
    defer fmt.Println("defer 中的 x =", x)  // 此时 x=10 被记录
    x = 20
    fmt.Println("函数中的 x =", x)
}
// 输出:
// 函数中的 x = 20
// defer 中的 x = 10
```

### 6.4 典型应用场景

**资源清理**（最常见用途）：

```go
func readFile(filename string) error {
    file, err := os.Open(filename)
    if err != nil {
        return err
    }
    defer file.Close()  // 确保文件一定会被关闭
    
    // 处理文件...
    return nil
}
```

**互斥锁释放**：

```go
func safeOperation() {
    mu.Lock()
    defer mu.Unlock()  // 确保锁一定会被释放
    
    // 临界区操作...
}
```

**数据库连接**：

```go
func queryDB() error {
    conn, err := db.Connect()
    if err != nil {
        return err
    }
    defer conn.Close()  // 确保连接一定会被关闭
    
    // 数据库操作...
    return nil
}
```

### 6.5 defer 的优势

1. **位置清晰**：打开资源后立即写关闭逻辑，不会遗忘
2. **保证执行**：即使发生 panic，defer 也会执行
3. **代码简洁**：不需要在每个 return 前手动清理

---

## 7. 对比其他语言

| 特性 | Go | Java/C | Python |
| --- | --- | --- | --- |
| 条件括号 | 不需要 | 需要 | 不需要 |
| 条件花括号 | 必须 | 必须 | 不需要（用缩进） |
| switch break | 不需要（自动） | 需要 | N/A（用 match） |
| 循环关键字 | 只有 for | for/while/do-while | for/while |
| 资源清理 | defer | try-finally | with 语句 |

---

## 8. 本章小结

| 语句 | 要点 |
| --- | --- |
| `if` | 条件不加括号，花括号必须；支持初始化语句 |
| `switch` | 不需要 break；支持多值匹配、无表达式、类型 switch |
| `for` | 唯一循环关键字；三段式/while风格/无限循环/range |
| `break/continue` | 支持带标签，用于控制外层循环 |
| `defer` | 延迟到函数返回前执行；LIFO 顺序；用于资源清理 |

**核心记忆**：

1. Go 的 `switch` 自动 break，需要穿透用 `fallthrough`
2. Go 只有 `for` 循环，但能实现所有循环形式
3. `defer` 是资源管理的最佳实践，"打开后立即 defer 关闭"

---

## 9. 动手练习

1. 使用带初始化语句的 `if` 判断一个数是正数、负数还是零
2. 用无表达式 `switch` 实现成绩等级判断（A/B/C/D/F）
3. 使用 `for range` 遍历字符串 `"Hello, 世界"`，观察中英文字符的索引差异
4. 编写一个函数，使用 `defer` 模拟文件的打开和关闭操作
5. 使用带标签的 `break` 实现：在二维数组中查找某个值，找到后立即退出所有循环
